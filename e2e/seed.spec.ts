/* eslint-disable @typescript-eslint/no-unused-vars */
import { test, expect } from './fixtures';

test.describe('Test group', () => {
	test('seed', async ({ phlickClient }) => {
		// generate code here.
	});
});
